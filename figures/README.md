# Figure Outputs

This directory contains generated vector PDF figures from:

```bash
python generate_figures.py --root .
```

The figures are deterministic outputs of the experiments in `generate_figures.py`. Regenerate the figures together with the files in `data/` whenever the numerical parameters or diagnostics change.
